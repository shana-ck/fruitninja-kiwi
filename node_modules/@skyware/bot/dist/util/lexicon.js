export function is(lexicon, obj) {
    return typeof obj === "object" && obj !== null && "$type" in obj
        && (obj.$type === lexicon || obj.$type === lexicon + "#main");
}
export const asDid = (did) => {
    if (!did.startsWith("did:"))
        throw new Error("Invalid DID: " + did);
    return did;
};
